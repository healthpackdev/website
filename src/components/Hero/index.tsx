import HeroAbout from '@components/Hero/hero-about';

const Hero: React.FC = () => (
  <section id="hero" className="flex items-center lg:my-20 my-16">
    <HeroAbout />
  </section>
);

export default Hero;
